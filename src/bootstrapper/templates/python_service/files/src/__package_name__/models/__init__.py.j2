"""Model package.

Every model must be imported here: Alembic autogenerate only sees tables that
are attached to `Base.metadata` at import time.
"""

from __future__ import annotations

from ..db.base import Base
from .item import Item

__all__ = ["Base", "Item"]
