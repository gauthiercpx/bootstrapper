"""Built-in templates.

Every submodule here exposes a `COMPONENTS` list, which the registry collects at
startup. Adding a template is: create a package, drop files under `files/`,
declare the component.
"""
